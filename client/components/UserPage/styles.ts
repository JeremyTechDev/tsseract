import { makeStyles } from '@material-ui/core';

const styles = makeStyles((theme) => ({
  user: {
    left: '15%',
    position: 'fixed',
    top: '14vh',
  },
  userCard: {
    margin: 5,
  },
  divider: {
    margin: '15px auto',
  },
}));

export default styles;
